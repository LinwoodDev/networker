name: networker_ble
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.3 <3.0.0'

dependencies:
  networker:
    path: ../networker
  quick_blue:
    git:
      url: https://github.com/woodemi/quick.flutter
      ref: b6954f9acae12da069092dffec4f14baaae2bddc
      path: packages/quick_blue
dependency_overrides:
  bluez: ^0.8.0
dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.4
